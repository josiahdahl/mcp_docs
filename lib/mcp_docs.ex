defmodule MCPDocs do
  @moduledoc """
  MCPDocs provides a Model Context Protocol (MCP) server for Elixir projects.

  This allows language models to query documentation and type information
  about functions and modules in an Elixir project, similar to IEx's `h` helper.
  """
end
